// src/app/account/setup/page.tsx
// Account setup page. This is where the user can setup their account when they first sign in. 
// TODO: maybe in the future there will be an API endpoint from HM so we don't have to do this... 
// This page will ask the user if they are a student or teacher. Then, it'll ask the user to input 
// their schedule. 
// TODO: Design this page(s) on Figma

export default function AccountSetupPage() {
    return (
        <div>
            <h1>Account Setup</h1>
        </div>
    );
}